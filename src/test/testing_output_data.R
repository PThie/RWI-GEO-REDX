testing_output_data <- function(output_data_current = NA, output_data_previous = NA) {

    #--------------------------------------------------
    # calculate overall mean (including all time periods)

    # mean_overall_current <- mean(output_data_current$timeeff)
    # mean
    # #--------------------------------------------------
    # # calculate mean only for periods that are present in both datasets

    # sapply(output_data_current, class)

}